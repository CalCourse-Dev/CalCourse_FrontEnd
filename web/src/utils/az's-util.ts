export const parse_search_string = (search_string: string): string => {
    // if (search_string.length === 5) {
    //     return search_string as number as string
    // }

    if (search_string.includes("MCB")) {
        
    }



    return search_string
}